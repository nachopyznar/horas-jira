{
  "hooks": {
    "get-hooks": "deno run -q --allow-read --allow-net https://deno.land/x/deno_slack_hooks@1.1.0/mod.ts"
  },
  "config": {
    "trigger-paths": ["triggers/*.ts"],
    "workflow-paths":["workflows/*.ts"]
}
}